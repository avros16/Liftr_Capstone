import React from 'react'
import BackContainer from './BackContainer'

function Back ({back, user}) {
    return (
  <div>


<BackContainer user={user} back={back}/>



  </div>
    );
  }
  
  export default Back;